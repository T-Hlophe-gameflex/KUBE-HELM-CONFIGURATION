#!/usr/bin/env bash
# Archived: original copy of scripts/update_awx_survey_records.sh
# The canonical survey population script is now: automation/scripts/update-awx-surveys.sh

cat <<'EOF'
This file is an archived copy of `scripts/update_awx_survey_records.sh`.
It was moved to `automation/archive/` during consolidation. Use
`automation/scripts/update-awx-surveys.sh` to populate AWX surveys from
Cloudflare zones and records.
EOF
