---
- hosts: localhost
  gather_facts: false
  tasks:
    - name: noop placeholder for platform-workflow (archived)
      debug:
        msg: "Placeholder playbook for platform-workflow.yml (archived). See automation/archive/ for original."
